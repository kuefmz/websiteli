<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="canonical" href="/en/blog/" />
    <script is:inline>
      try {
        window.sessionStorage?.setItem("websiteli_redirect_context", JSON.stringify({
          landingPage: window.location.href,
          referrer: document.referrer || "",
        }));
      } catch {}
      const target = new URL("/en/blog/", window.location.origin);
      target.search = window.location.search;
      target.hash = window.location.hash;
      window.location.replace(target.toString());
    </script>
    <title>Websiteli Blog</title>
  </head>
  <body>
    <a href="/en/blog/">Continue to Websiteli Blog</a>
  </body>
</html>
